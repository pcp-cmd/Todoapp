package com.todoapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── Claude-inspired palette ──

// Light mode
val ClaudeLightBg         = Color(0xFFFAF9F5)   // warm ivory background
val ClaudeLightSurface    = Color(0xFFFFFFFF)    // clean white cards
val ClaudeLightSurfaceVar = Color(0xFFF0EDE6)    // warm beige surface variant
val ClaudeLightOnBg       = Color(0xFF2D2B27)    // deep warm brown text
val ClaudeLightOnSurface  = Color(0xFF3C3935)    // card text
val ClaudeLightSecondary  = Color(0xFF5A5A5A)    // secondary text - deeper for better contrast
val ClaudeLightOutline    = Color(0xFFE0DCD4)    // borders / dividers
val ClaudePrimary         = Color(0xFFC66B4D)    // deeper orange — better contrast

// Dark mode
val ClaudeDarkBg         = Color(0xFF1A1917)     // near-black warm bg
val ClaudeDarkSurface    = Color(0xFF26241F)     // elevated cards
val ClaudeDarkSurfaceVar = Color(0xFF33302B)     // surface variant
val ClaudeDarkOnBg       = Color(0xFFECE8E1)     // warm light text
val ClaudeDarkOnSurface  = Color(0xFFD9D5CD)     // card text
val ClaudeDarkSecondary  = Color(0xFF8A857D)     // secondary text
val ClaudeDarkOutline    = Color(0xFF3D3A36)     // borders / dividers
val ClaudePrimaryDark    = Color(0xFFE89A6C)     // lighter terracotta for dark

// ── Category color presets ──
val CategoryColors = listOf(
    Color(0xFFC66B4D),  // deeper orange (Claude signature)
    Color(0xFF6B8F71), // sage green
    Color(0xFF7B8CBA), // dusty blue
    Color(0xFFB07BAC), // muted purple
    Color(0xFFC4786E), // salmon
    Color(0xFF8B9E6B), // olive
    Color(0xFFD4A853), // warm gold
    Color(0xFF6E9BA0), // teal
)
